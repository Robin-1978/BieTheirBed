# Named API accounts/endpoints. Cloud accounts and their keys belong in the
# per-user ~/.pc-assistant/config/local.yaml file, outside the source tree.
providers:
  local_llama:
    driver: "llamacpp"
    server_url: "http://127.0.0.1:8192"
    requires_api_key: false
    timeout: 120

# Stable application aliases mapped to upstream model identifiers.
models:
  local_default:
    provider: "local_llama"
    model: ""
    supports_vision: true
    context_window: 8192

default_model: "local_default"
fallback_enabled: true
fallback_model: ""

# Single-model fallback fields used only when providers/models are absent.
llm_provider: "llamacpp"
llm_server_url: "http://127.0.0.1:8192"
llm_model_name: ""
llm_api_key: ""
llm_api_base: ""
llm_temperature: 0.7
llm_timeout: 120
max_iterations: 32
max_tokens: 4096
shell_timeout: 30
context_window_budget: 8192
trace_enabled: true
llm_trace_log: "logs/llm_trace.jsonl"
turn_trace_log: "logs/turn_trace.jsonl"
log_file: "logs/pc_assistant.json"
runtime_root: "~/.pc-assistant"
working_directory: "."
service_host: "127.0.0.1"
service_port: 9527
service_token: ""
webhook_enabled: false
webhook_host: "127.0.0.1"
webhook_port: 9528
webhook_routes: {}
feishu_enabled: false
feishu_app_id: ""
feishu_app_secret: ""
feishu_receive_id: ""
attachment_ttl_seconds: 3600
attachment_cleanup_interval_seconds: 300
supports_vision: null

# Dynamic MCP tools are disabled unless a private local config explicitly
# enables a server and assigns local effect/capability/risk policy per tool.
mcp_servers: {}

# Additional roots containing Skill package directories. Knoa also loads its
# packaged built-in Skills and ~/.pc-assistant/skills.
skill_directories: []
