# 小诺产品剩余工作清单（2026-08-23 结转）

> 状态：本次结转项已于 2026-08-24 实施完成，保留本文作为范围与验收记录。
> 本文原记录 0.2.84 发布后未完成的产品计划项，按优先级排列。
> 活动勾选清单见 `docs/knoa-product-todo.md`；本文是按工作量组织的推进视图。
> 方针：正向设计、高内聚低耦合、YAGNI；每项独立设计、独立提交。
> 实施方案见[小诺产品剩余 Backlog 方案设计](knoa-product-backlog-solution-design-2026-08-23.md)。

## 大项（需要独立设计周期）

以下六项均已完成实现；生产环境的真实 FCM 投递仍需有效 Service Account 与 Android Firebase 配置后做真机验收。

### 1. App 关闭后的系统级远程推送（P1.2 核心，已完成实现）

- 现状：通知为 App 在线事件流 + expo-notifications 本地通知；文案已如实标注「App 运行期间送达」，设置页有测试通知。
- 缺口：App 完全关闭、进程被杀后，任务完成/失败/审批无法到达手机。
- 方向：Core 持久化 Notification Intent（完成、失败、审批、补充信息、Node 离线、需要更新）作为唯一事实源；接入 FCM/APNs 级远程推送；App、飞书、钉钉、桌面复用同一事实，支持去重、深链、测试和过期。
- 关键决策：推送服务归属（Hub 直推 or 第三方推送网关）、密钥管理、通知与事件游标的一致性。

### 2. 交互式浏览器会话产品化（P1.6，已完成）

- 现状：AI 已可调用 web_search/web_fetch 和桌面截图/窗口/键鼠工具；Codex 通过受控 Platform MCP 使用获授权能力。
- 缺口：浏览器启动、导航、页面读取、点击/输入、下载跟踪和持久会话没有产品化入口；在实现前不得向用户暗示系统能稳定处理网页工作。
- 方向：实现独立、可安装的第一方 Browser MCP 参考包，作为官方可选能力和第三方 MCP 包样板；先做会话/导航/语义读取/下载四个可验收动作，再扩展交互与持久 Profile。

### 3. 扩展一键安装能力（P1.4，已完成）

- 现状：Skill/MCP 支持本地/远程导入和 inspect/draft/publish。
- 缺口：用户仍需理解 server id、内部路径；没有预检权限、启用/停用、更新和回滚的一键流程。
- 方向：能力包格式（清单 + 权限声明 + allowlisted 声明式预检）、导入即预检、失败可回滚，默认不要求填写内部对象；不允许任意预检脚本。

### 4. Webhook/MCP Resource 用户流程（P1.5 残留，已完成）

- 现状：TaskLaunchEditor 可选 MCP Resource；后端 Trigger 存在。
- 缺口：创建、复制地址、生成密钥、测试触发、暂停和查看最近事件的用户流程。

### 5. 自我进化闭环（P2，已完成受控范围）

- 现状：有评测和候选设计。
- 缺口：用户反馈/失败证据 → 失败分类 → 评测集 → 候选 → 沙箱回放 → 人工批准 → 灰度 → 监控 → 回滚的完整控制面。
- 边界（已有设计约束）：允许自动化的范围不含权限上限、审批策略、凭据、隔离边界和安全等级。

### 6. 能力包市场与版本治理（P2，已完成签名静态目录范围）

- 依赖：第 3 项的一键安装先落地；不引入通用 Workflow DSL 或企业治理中心。

## 中项（单次提交可完成）

1. [x] **握手阶段细分打点**：已提供阶段诊断与链路切换历史。
2. [x] **scheduled/event 触发路径预检时机**：统一进入 Core 启动边界。
3. [x] **任务创建文件夹级输入**：Android SAF bounded snapshot 与 immutable manifest 已落地。
4. [x] **Chat 失败后编辑重发、回到最新**：已落地。
5. [x] **执行详情页统一摘要组件**：`WorkResultSummary`、结构化修改事实和一键继续已落地。
6. [x] **Console 一键修复**：诊断修复、重查与日志入口已落地。
7. [x] **Console 版本信息补全**：源码 commit 与配置 revision 已落地。
8. [x] **链路切换历史**：已落地。

## 已知问题处理结果

1. [x] **stop-while-starting 竞态**：已用持久取消 latch 和 active invocation 注册边界消除空窗。
2. [x] **Windows/POSIX 测试边界**：已增加平台合同 marker，并在统一进程输出 adapter 中支持 UTF-8、GB18030、首选编码和原始 bytes 审计摘要；Windows 原生矩阵仍应由 Windows CI 持续执行。

## 已完成不再列入

缓存优先全页面覆盖、链路竞速、连接耗时与承载链路记录、结果结构化交付、创建即执行预检、任务附件、解绑自动清理、测试通知、Console 三类状态、分类缓存清除——见 `docs/knoa-product-todo.md` 勾选记录。
